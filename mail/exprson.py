# 1. A large file contains a list of mobile numbers. Print the list of
# VIP numbers in this file. A VIP number meets any of these criteria
# 1. First 5 digits and last 5 digits are identical ex: 9810198101
# 2. First 5 digits and last 5 digits are in a series, either
# increasing or decreasing. For example: 9810098101, 9811198110
# 3. Has 4 or more repeating digits. For example: 9812222910
# 4. The first 5 and last 5 digits are identical with a pair of
# digits flipped. Example: 9923999329
#
import re
# with open('vipmobile','r') as f:
#     for i in f:
#         for a in i.split(' '):
#             if(re.findall("^....."))==(re.findall(".....$")):
#                 print(f"{a} is the VIP number")
# library that handles the URL stuff

import re
# with open('vipmobile','r+') as f:
#     for line in f:
#         for word in line.split():
#             if(word == int
fruits = ("apple", "banana", "cherry")	#Multiply
mytuple = fruits * 2
print(mytuple)

# for line in fhand:
#     # Getting the text file
#     # content line by line.
#     s = line.decode().strip()
#
#     # regex for extracting all email-ids
#     # from the text file
#     reg = re.findall(r"[A-Za-z0-9._%+-]+"
#                     r"@[A-Za-z0-9.-]+"
#                     r"\.[A-Za-z]{2,4}", s)
#
#     # printing the list output
#     print(reg)



